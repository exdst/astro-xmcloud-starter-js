---
/**
 * LocationSearchItem Astro wrapper.
 * This component is rendered as part of the React island (LocationSearch),
 * so this wrapper is provided only for completeness / standalone usage.
 * In normal operation, LocationSearchItem.tsx is used directly within the React tree.
 */
---
<slot />
